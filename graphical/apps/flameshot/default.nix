{
  services.flameshot = {
    enable = true;
    settings = {
      General.uiColor = "#285577";
      # Create this folder if it doesn't already exist, otherwise you'll get da error
      General.savePath = "/home/amadeusw/Pictures/Screenshots";
    };
  };
}
